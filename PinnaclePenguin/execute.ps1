# Navigate into the server directory
cd server

# Install dependencies in the server directory
npm install

# Navigate back to the parent directory
cd ..

# Assuming 'client' is a directory at the same level as 'server'
# Navigate into the client directory
cd client

# Install dependencies in the client directory
npm install

# Navigate back to the main directory where you might want to start the main application
cd ..

npm install